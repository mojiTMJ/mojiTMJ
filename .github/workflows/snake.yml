name: 🐍 Generate contribution snake

on:
  schedule:
    - cron: "0 */12 * * *"     # every 12h
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake SVG
        uses: Platane/snk/svg-only@d8f6715049803e982ee5ff501b6b9b7d5deeb09b # v3, pinned 2026-08-25
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=22D3EE&color_dots=02030A,3A0F3C,693064,90306F,EC4899
            dist/github-snake-dark.svg?palette=github-dark&color_snake=22D3EE

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@5859b4ea7efe6b470a5b19c570460eae9323aefc # v3, pinned 2026-08-25
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
